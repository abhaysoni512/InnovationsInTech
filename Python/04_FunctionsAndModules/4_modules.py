#library : using somebody code which is already wrriten
# There are two types of modules
# i.in built modules
# ii. external
import math     #math is modules(library)
import myModules

print(math.sqrt(4))
myModules.hello()